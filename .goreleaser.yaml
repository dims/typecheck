version: 2

project_name: typecheck

before:
  hooks:
    - go mod tidy
    - go generate ./...

builds:
  - env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    binary: typecheck
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}

archives:
  - name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    files:
      - README.md
      - LICENSE

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - '^docs:'
      - '^test:'

dockers:
  - image_templates:
      - "ghcr.io/dims/typecheck:{{ .Tag }}-amd64"
      - "ghcr.io/dims/typecheck:v{{ .Major }}-amd64"
      - "ghcr.io/dims/typecheck:v{{ .Major }}.{{ .Minor }}-amd64"
      - "ghcr.io/dims/typecheck:latest-amd64"
    dockerfile: Dockerfile
    use: buildx
    push_flags:
      - "--attest"
      - "type=sbom"
      - "--attest"
      - "type=provenance"
    build_flag_templates:
      - "--pull"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--label=org.opencontainers.image.source={{.GitURL}}"
      - "--platform=linux/amd64"
      - "--provenance=false"
  - image_templates:
      - "ghcr.io/dims/typecheck:{{ .Tag }}-arm64"
      - "ghcr.io/dims/typecheck:v{{ .Major }}-arm64"
      - "ghcr.io/dims/typecheck:v{{ .Major }}.{{ .Minor }}-arm64"
      - "ghcr.io/dims/typecheck:latest-arm64"
    dockerfile: Dockerfile
    use: buildx
    goarch: arm64
    push_flags:
      - "--attest"
      - "type=sbom"
      - "--attest"
      - "type=provenance"
    build_flag_templates:
      - "--pull"
      - "--label=org.opencontainers.image.created={{.Date}}"
      - "--label=org.opencontainers.image.title={{.ProjectName}}"
      - "--label=org.opencontainers.image.revision={{.FullCommit}}"
      - "--label=org.opencontainers.image.version={{.Version}}"
      - "--label=org.opencontainers.image.source={{.GitURL}}"
      - "--platform=linux/arm64"
      - "--provenance=false"

docker_manifests:
  - name_template: "ghcr.io/dims/typecheck:{{ .Tag }}"
    image_templates:
      - "ghcr.io/dims/typecheck:{{ .Tag }}-amd64"
      - "ghcr.io/dims/typecheck:{{ .Tag }}-arm64"
  - name_template: "ghcr.io/dims/typecheck:v{{ .Major }}"
    image_templates:
      - "ghcr.io/dims/typecheck:v{{ .Major }}-amd64"
      - "ghcr.io/dims/typecheck:v{{ .Major }}-arm64"
  - name_template: "ghcr.io/dims/typecheck:v{{ .Major }}.{{ .Minor }}"
    image_templates:
      - "ghcr.io/dims/typecheck:v{{ .Major }}.{{ .Minor }}-amd64"
      - "ghcr.io/dims/typecheck:v{{ .Major }}.{{ .Minor }}-arm64"
  - name_template: "ghcr.io/dims/typecheck:latest"
    image_templates:
      - "ghcr.io/dims/typecheck:latest-amd64"
      - "ghcr.io/dims/typecheck:latest-arm64"

release:
  github:
    owner: dims
    name: typecheck
  draft: false
  prerelease: auto
  mode: replace
  header: |
    ## Release {{ .Tag }} ({{ .Date }})
    
    This release includes binaries for multiple platforms and a container image.
  footer: |
    **Full Changelog**: https://github.com/dims/typecheck/compare/{{ .PreviousTag }}...{{ .Tag }}